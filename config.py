"""
Конфигурация для всех модулей TEMA

Содержит настройки для OpenAI API, Mathpix API, промпты и параметры обработки.
"""

# OpenAI API настройки для chunking (beautify_topics)
CHUNKING_OPENAI_MODEL = "gpt-4o"
CHUNKING_TEMPERATURE = 0.2
CHUNKING_TOP_P = 0.8
CHUNKING_FREQUENCY_PENALTY = 0
CHUNKING_PRESENCE_PENALTY = 0

# Параметры токенизации для chunking
DEFAULT_TOKENIZATION_MODEL = "gpt-3.5-turbo"
DEFAULT_WARN_TOKENS = 2000

# Паттерны для извлечения оглавления
TOC_START_PATTERNS = [
    r"СОДЕРЖАНИЕ",
    r"Оглавление",
    r"МАЗМҰНЫ",  # kazakh
    r"Contents"
]
TOC_END_PATTERNS = [
    r"Учебное издание",
    r"Глоссарий",
    r"Ответы",
    r"Библиография"
]
TOC_MIN_LENGTH = 50  # Минимальная длина оглавления в символах

# System prompt для обработки оглавления
TOPICS_BEAUTIFY_PROMPT = """
Ты — редактор и структурировщик оглавлений учебников. Преобразуй список тем из оглавления в чистый, структурированный и лаконичный список, убери дубли, лишние символы, номера страниц, повторяющиеся слова, оставь только суть тем. Не добавляй ничего от себя. Верни результат в виде текста (каждая тема — отдельная строка, без нумерации и лишних символов).
"""

# OpenAI API настройки для retrieval (извлечение задач)
RETRIEVAL_OPENAI_MODEL = "o4-mini"
RETRIEVAL_TEMPERATURE = 0
RETRIEVAL_TOP_P = 0.4
RETRIEVAL_FREQUENCY_PENALTY = 0
RETRIEVAL_PRESENCE_PENALTY = 0

# System prompt для извлечения задач из учебников
RETRIEVAL_SYSTEM_PROMPT = """
Ты — умный и точный парсер учебников по математике.
Цель: извлекать ТОЛЬКО самодостаточные, осмысленные задачи (без внешних ссылок и недостающих данных).

1) Поиск задач
   Считай задачей фрагмент, который:
   - начинается с номера: `№?\s*\d+[)\.]?`, например: `1)`, `25.`, `122*)`, `№ 7.`
   - ИЛИ содержит глагол-метку: «Задача», «Решите», «Вычислите», «Найдите», «Докажите», «Постройте», «Составьте», «Определите».
   Границы задачи — от её начала до следующего номера/метки или до разделов «Решение», «Ответ», «Дано», «Подсказка».

2) Жёсткие фильтры валидности (valid)
   Включай в результат ТОЛЬКО если одновременно:
   - нет ссылок типа «по рисунку/чертежу/таблице/схеме/графику/рис. 1.2»;
   - нет отсылок к «вариантам а), б), в)» без их полного текста;
   - присутствуют данные, достаточные для решения: есть числа/условия/обозначенные переменные или чёткое требование доказательства/построения;
   - это не определение/теория/пример/пояснение;
   - формулировка самодостаточна вне контекста страницы.
   Примеры НЕвалидных (НЕ включай):
   - «Во сколько раз число прочитанных страниц больше непрочитанных?» (нет данных);
   - «Составьте все возможные отношения … на рисунке 1.2» (нужен рисунок);
   - чисто риторические вопросы «Какую часть составляет…?» без чисел.

3) Извлечение полей
   - "id": строка. Если есть номер, возьми его как есть (с суффиксами типа `122*`). Если номера нет — сгенерируй "auto-1", "auto-2", …
   - "original": полный текст условия БЕЗ разделов «Решение» и «Ответ».
   - "solution": пошаговое решение, только если явно присутствует после «Решение». Иначе null. Ничего не выдумывай.
   - "answer": текст после «Ответ:»/«Ответ.»/«Ответ — …». Если нет — null.
   - "topic": выбери ТОЛЬКО из списка ТЕМ НИЖЕ; верни точное имя темы. Если не подходит — выбери ближайшую.
   - "difficulty": A — простой одношаговый счёт/пропорции; B — несколько шагов/смешанные приёмы; C — доказательства/сложные конструкции/задачи с параметрами.
   - "tags": 3–9 ключевых существительных/методов (напр.: «отношения», «пропорции», «проценты», «вероятность», «ломаная», «масса», «система уравнений»).
   - "type": "Практическая" (посчитать/построить/найти численно) или "Теоретическая" (доказать/сформулировать/обосновать).

4) Темы (примерный список — замени на свой при необходимости):
   ["Отношения и пропорции","Доли и проценты","Вероятность","Алгебраические уравнения","Системы уравнений","Неравенства","Функции и графики","Геометрия: треугольники","Геометрия: многоугольники","Геометрия: окружность","Площади и объёмы","Комбинаторика","Последовательности","Логика и доказательства"]

5) LaTeX
   Все формулы оформи строго в `$...$` (inline или block). Следи за синтаксисом (`\\frac`, `\\cdot`, `\\text{}` и т.п.).

6) Вывод
   - Верни ТОЛЬКО JSON-массив объектов (без комментариев и текста вокруг).
   - Если валидных задач нет — верни `[]`.

Если тема не подходит — выбери ближайшую по смыслу.
"""

# OpenAI API настройки для generator (генерация новых задач)
GENERATOR_OPENAI_MODEL = "o4-mini"
GENERATOR_TEMPERATURE = 0.5
GENERATOR_SEED = 42

# System prompt для генерации новых задач
GENERATOR_SYSTEM_PROMPT = """Ты — генератор учебных заданий. Твоя задача — на основе предоставленного примера задачи создать новую задачу того же предмета и уровня сложности, но с другими числами и переформулированной формулировкой и контекстом. Задача не должна быть копией, а должна быть переформулирована с изменениями, сохраняя структуру и сложность.

1. **Задача должна быть самодостаточной:**
   - Не генерируй задачи, которые требуют дополнительной информации, такие как графики, рисунки, схемы и т.д. Все необходимые данные должны быть представлены в задаче.
   - Задача не должна полагаться на приближения, округления или неопределенности. Все числа и результаты должны быть точными.

2. **Отсутствие округлений:**
   - Если задача предполагает использование чисел, то они должны быть выражены точно. В случае с пропорциями или делением избегай округления чисел.

3. **Математическая точность:**
   - Формулы и решения должны быть точными, с использованием корректного математического синтаксиса в LaTeX. Никаких приближений или округлений в расчетах.
   - Формулы должны быть оформлены только в виде `$...$` (inline) или `$$...$$` (block). Все команды LaTeX, такие как `\frac`, `\cdot`, `\text`, должны быть синтаксически корректными.

4. **Пошаговое решение:**
   - Решение задачи должно быть пошаговым, с подробным пояснением каждого шага. Объясни все математические операции и использованные формулы.

5. **Структура ответа:**
   - `original`: Переформулированная задача с новыми числами и формулировкой.
   - `solution`: Пошаговое решение задачи с объяснениями.
   - `topic`: Тема задачи.
   - `answer`: Окончательный ответ (точный).
   - `type`: Тип задачи (например, расчет, текстовая задача и т.д.).
   - `tags`: Теги, характеризующие задачу.
   - `difficulty`: Уровень сложности задачи (например, легкий, средний, сложный).

Верни только JSON-объект с полями 'original', 'solution', 'topic', 'answer', 'type', 'tags', 'difficulty'. Никаких комментариев или пояснений вне JSON.
"""

# OCR настройки
OCR_CONFIDENCE_THRESHOLD = 0.6
OCR_LANGUAGE = "rus+eng"
IMAGE_DPI = 300
IMAGE_RESIZE_FACTOR = 1.5

# Поддерживаемые форматы файлов для OCR
SUPPORTED_IMAGE_FORMATS = [".jpg", ".jpeg", ".png", ".bmp", ".tiff", ".tif"]
SUPPORTED_PDF_FORMATS = [".pdf"]

# Максимальный размер файла для OCR (в байтах)
MAX_FILE_SIZE = 50 * 1024 * 1024  # 50 MB

